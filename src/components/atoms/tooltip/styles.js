import { css } from '@emotion/core'

export const contentStyle = { width: 'auto' }

export const buttonStyle = css`
  outline: 0;
  border: 0;
  background: transparent;
  height: auto;
  width: auto;
  margin: 0;
  padding: 0;
  cursor: pointer;
`
